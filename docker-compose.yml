version: "3.9"
services:
  db:
    image: postgres:16
    restart: always
    environment:
      POSTGRES_USER: alok
      POSTGRES_PASSWORD: alok123
      POSTGRES_DB: hedge_db
    ports:
      - "5432:5433"
    volumes:
      - ./pgdata:/var/lib/postgresql/data
